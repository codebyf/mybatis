<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.byf.dao.IUserDao">
    <!-- 配置查询所有 -->
    <select id="findAll" resultType="com.byf.domain.User">
        select * from user
    </select>
    <insert id="saveUser" parameterType="com.byf.domain.User">
    <!-- 配置插入操作后，获取插入数据的id -->
        <selectKey keyProperty="id" keyColumn="id" resultType="int" order="AFTER">
            select LAST_INSERT_ID();
        </selectKey>
        insert into user(username,address,sex,birthday)
        values(#{username},#{address},#{sex},#{birthday});
    </insert>
    <update id="updateUser" parameterType="com.byf.domain.User">
        update user
        set username=#{username},address=#{address},sex=#{sex},birthday=#{birthday}
        where id=#{id}
    </update>
    <delete id="deleteUser" parameterType="java.lang.Integer">
        delete from user
        where id=#{uid}
    </delete>

    <select id="findById" parameterType="INT" resultType="com.byf.domain.User">
        select * from user
        where id=#{uid}
    </select>

    <select id="findByName" parameterType="string" resultType="com.byf.domain.User">
        <!--select * from user
        where username like #{userName}
        Preparing: select * from user where username like '%J%'
        Statement 拼接
        -->
        select * from user
        where username like '%${value}%'
        <!-- Preparing: select * from user where username like ?
         PrepareStatement 占位符实现 -->
    </select>
    <select id="findTotal" resultType="int">
        select count(*) from user
    </select>

    <select id="findByQueryVo" parameterType="com.byf.domain.QueryVO" resultType="com.byf.domain.User">
        <!--select * from user
        where username like #{userName}
        Preparing: select * from user where username like '%J%'
        Statement 拼接
        -->
        select * from user
        where username like #{user.username}
        <!-- Preparing: select * from user where username like ?
         PrepareStatement 占位符实现 -->
    </select>
</mapper>